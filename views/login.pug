doctype html
html
	head
		if userLogged
			script.
				window.location.replace = "/";
		title Matcha | #{title}
		include header_items.pug
		link(rel='stylesheet', href='/login.css')
	body
		include navbar.pug

		#Main.container.mx-auto
			h1.text-center.mt-white.mt-5.display-3 Welcome Back!
			.row
				.col
					.card.my-5.mx-auto(style='max-width:500px')
						.card-body
							block content
								h1 Login
								hr
								form#login.needs-validation(name="login", action='', method='POST' novalidate)
									.form-group
										label(for='userName') Username:
										input#userLogin.form-control(type='text' placeholder='Enter Username' name='userLogin' required)
										.valid-feedback Valid.
										.invalid-feedback Please fill out this field.

									.form-group
										label(for='password') Password:
										input#userPass.form-control(type='password' placeholder='Enter Password' name='userPass' required)
										.valid-feedback Valid.
										.invalid-feedback Please fill out this field.
									
									p.text-right
										| New User? 
										a(href='/user/register') Register.
									p.text-right
										| Forgot Password? 
										a(href='/forgot') Reset.

									button#submit.btn.btn-success.btn-block.btn-lg.enableOnInput(type='submit', value='add' disabled='disabled') Login
									//- input#submit.btn.btn-success.btn-block.btn-lg.enableOnInput(type='button', value='Login' onclick='LogInCheck()' style="background-color:#28a745")
								p#error.mt-3
		
		include footer.pug

			//- SCRIPTS

		include bootstrap_js.pug
		script(src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js")
		script.
			$(document).ready(function(){
				var userLogin,userPass;
				$("#submit").click(function(){
				userLogin=$("#userLogin").val();
				userPass=$("#userPass").val();
				$.post("/",{userLogin: userLogin,userPass: userPass}, function(data){
				});
				window.location.replace("http://localhost:8080?action=login");
				});
			});

		script(src="/submission.js")
		//- script(src="/login.js")

		//- script.
		//- 	function LogInCheck() {
		//- 		// const uname = document.getElementById('userLogin');
		//- 		console.log("FFS");
		//- 	};
		//- script.
		//- 	$('#userPass, #userLogin').keyup(function () {
		//- 		if ($(this).val() == '') {
		//- 			//Check to see if there is any text entered
		//- 			// If there is no text within the input ten disable the button
		//- 			$('.enableOnInput').prop('disabled', true);
		//- 		} else {
		//- 			//If there is text in the input, then enable the button
		//- 			$('.enableOnInput').prop('disabled', false);
		//- 		}
		//- 	});

		script.
			$("input[type='text'], input[type='password']").on("keyup", function(){
				if($("input[type='text']").val() != "" && $("input[type='password']").val() != ""){
					$("#submit").prop("disabled", false);
				} else {
					$("#submit").prop("disabled", true);
				}
			});
